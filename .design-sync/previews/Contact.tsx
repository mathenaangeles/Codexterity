import { Contact } from "codexterity";

// Contact section: two-column layout with info panel and the inquiry form
// (name / email / company / service select / message).
export const Default = () => <Contact />;
